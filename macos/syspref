#!/usr/bin/env bash

# To see available preferences options:
# => ls /System/Library/PreferencePanes/
# For example, for Security:
# => open "x-apple.systempreferences:com.apple.preference.security"

# We want the All:
open "x-apple.systempreferences:com.apple.preference.prefPane"
